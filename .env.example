# Example environment variables for EdgeLedger
# Copy this file to `.env` and fill in real values. Do NOT commit
# secrets to version control. Keep `.env` out of source control.

PLAIN_VAR=ExampleSite
# SECRET_VAR should remain secret and stored only in your local `.env`
SECRET_VAR=your_secret_here
